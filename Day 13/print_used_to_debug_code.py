#words_per_page = 0
pages = int(input("Number of pages: "))
print (pages)
words_per_page == (int(input("Number of words per page: ")))
print (words_per_page)
total_words = (pages * words_per_page)
print (total_words)

#The double = sign on line 4  if returning False instead of the number
#A single = sign should be used to correct the code.